import {NextFunction, Request, Response} from 'express';

export const getFuelRecord = async (
    req: Request,
    res: Response,
    next: NextFunction,
) => {};

export const createFuelRecord = async (
    req: Request,
    res: Response,
    next: NextFunction,
) => {};

export const deleteFuelRecord = async (
    req: Request,
    res: Response,
    next: NextFunction,
) => {};
